╔═══════════════════════════════════════════════════════════════════════╗
║     WIC2005 NETWORK AUTOMATION - TWO-PLAYBOOK STRUCTURE               ║
╚═══════════════════════════════════════════════════════════════════════╝

NEW PLAYBOOK STRUCTURE (2 PLAYBOOKS)
═══════════════════════════════════════════════════════════════════════

1. LAB SETUP PLAYBOOK (RECOMMENDED - FASTEST)
   ├─ File: ansible/lab_setup.yml
   ├─ Role: lab_setup (consolidated - 265 lines)
   ├─ Tasks:
   │  ├─ Configure hostnames (all devices)
   │  ├─ Setup OSPF routing (routers)
   │  ├─ Configure VLANs (switches)
   │  ├─ Setup DNS & NAT gateway (all devices)
   │  └─ Backup configurations
   ├─ Runtime: ~1-2 minutes
   └─ Perfect for: Initial lab setup

2. OPERATIONS PLAYBOOK (POST-LAB WITH TAG SELECTION)
   ├─ File: ansible/operations.yml
   ├─ Roles (selectable with tags):
   │  ├─ post_deployment_validation (tag: validation)
   │  ├─ telemetry_report (tag: telemetry)
   │  ├─ health_monitoring (tag: health)
   │  └─ security_verification (tag: security)
   ├─ Runtime: ~2-3 minutes (all) or ~1 min per tag
   └─ Perfect for: Post-deployment validation & ongoing operations

3. ORIGINAL PLAYBOOK (STILL AVAILABLE)
   └─ File: ansible/playbook.yml (backward compatible)

═══════════════════════════════════════════════════════════════════════

QUICK START - 3 SIMPLE STEPS
═══════════════════════════════════════════════════════════════════════

Step 1: Start Docker
  $ cd /home/engjunyuan/Documents/WIC2005
  $ docker compose up -d
  $ sleep 20

Step 2: Run Lab Setup (FASTEST - 1-2 MINUTES)
  $ cd ansible
  $ ansible-playbook -i inventory.yml lab_setup.yml -v

Step 3: Done! Lab is ready to use
  ✓ All 5 devices configured
  ✓ OSPF routing active
  ✓ VLANs configured
  ✓ Configs backed up

═══════════════════════════════════════════════════════════════════════

EXECUTION OPTIONS
═══════════════════════════════════════════════════════════════════════

LAB SETUP (FASTEST - RECOMMENDED)
  ansible-playbook -i inventory.yml lab_setup.yml -v

OPERATIONS - ALL TESTS
  ansible-playbook -i inventory.yml operations.yml -v

OPERATIONS - SPECIFIC TAGS (FLEXIBLE)
  ansible-playbook -i inventory.yml operations.yml --tags validation -v
  ansible-playbook -i inventory.yml operations.yml --tags health -v
  ansible-playbook -i inventory.yml operations.yml --tags security -v
  ansible-playbook -i inventory.yml operations.yml --tags telemetry -v

OPERATIONS - MULTIPLE TAGS
  ansible-playbook -i inventory.yml operations.yml --tags "health,security" -v

ORIGINAL PLAYBOOK (BACKWARD COMPATIBLE)
  ansible-playbook -i inventory.yml playbook.yml -v

═══════════════════════════════════════════════════════════════════════

NEW ROLE DETAILS
═══════════════════════════════════════════════════════════════════════

LAB_SETUP ROLE - Consolidates:
  ✓ router_config tasks
  ✓ switch_config tasks
  ✓ batch_update tasks
  ✓ backup_configs tasks

Location: ansible/roles/lab_setup/tasks/main.yml (265 lines)

Benefits:
  • Single role for all lab setup
  • Cleaner playbook structure
  • Faster (~1-2 minutes)
  • Individual roles still available for custom needs

═══════════════════════════════════════════════════════════════════════

FILES - PLAYBOOKS & ROLES
═══════════════════════════════════════════════════════════════════════

Main Playbooks:
  ✓ ansible/lab_setup.yml (13 lines)
  ✓ ansible/operations.yml (48 lines with improved tags)

Lab Setup Role:
  ✓ ansible/roles/lab_setup/tasks/main.yml (265 lines)

Documentation:
  ✓ PLAYBOOK_RESTRUCTURING.md (detailed explanation)
  ✓ README.md (updated with new 2-playbook structure)

═══════════════════════════════════════════════════════════════════════

VERIFICATION COMMANDS
═══════════════════════════════════════════════════════════════════════

# Verify docker containers
docker compose ps

# Test lab setup (dry-run)
cd ansible
ansible-playbook -i inventory.yml lab_setup.yml -v --check

# Run actual lab setup
ansible-playbook -i inventory.yml lab_setup.yml -v

# Check backups created
ls -la ../backups/

# Test OSPF after setup
docker compose exec router1 vtysh -c "show ip ospf neighbor"

═══════════════════════════════════════════════════════════════════════

KEY IMPROVEMENTS
═══════════════════════════════════════════════════════════════════════

✓ SIMPLER: Use lab_setup.yml instead of playbook.yml
✓ FASTER: Lab ready in 1-2 minutes instead of 3-4 minutes
✓ CLEARER: Obvious separation of setup vs. operations
✓ FLEXIBLE: Run setup, operations, or both separately
✓ ORGANIZED: Each playbook has clear purpose
✓ BACKWARD COMPATIBLE: Original playbook.yml still works
✓ MODULAR: Individual roles still available for custom use

═══════════════════════════════════════════════════════════════════════

DOCUMENTATION
═══════════════════════════════════════════════════════════════════════

Read for details:
  • PLAYBOOK_RESTRUCTURING.md (this restructuring)
  • README.md (updated - Playbook Execution Options section)

═══════════════════════════════════════════════════════════════════════

NEXT STEPS
═══════════════════════════════════════════════════════════════════════

1. Start Docker:
   docker compose up -d && sleep 20

2. Run lab setup:
   cd ansible && ansible-playbook -i inventory.yml lab_setup.yml -v

3. Verify it worked:
   docker compose exec router1 vtysh -c "show ip ospf neighbor"

4. (Optional) Run operations/monitoring:
   ansible-playbook -i inventory.yml operations.yml -v

═══════════════════════════════════════════════════════════════════════
